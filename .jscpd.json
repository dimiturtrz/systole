{
  "//": "Duplication detector (bd cardiac-seg-tte0) — the DRY axis, which coupling tools (import-linter/graph) can't see. The repo is already ~0.23% dup (python) / 0% (TS); this is a REGRESSION GUARD. threshold = max %% duplication before jscpd exits nonzero; kept as headroom over today's number. Advisory in CI until the Node step is proven stable, then graduate to blocking.",
  "path": ["core", "cardioseg", "cardioview/web/src"],
  "threshold": 1,
  "minTokens": 50,
  "gitignore": true,
  "ignore": ["**/node_modules/**", "**/dist/**", "**/__pycache__/**", "**/.staging/**", "baselines/**", "mri-sim/**"],
  "reporters": ["console"],
  "absolute": false
}
