# PLAN

Public roadmap for this project.

`PLAN.local` holds the private/sensitive plan — positioning, targets, and anything not for the
public repo. Keep public direction here; keep private context there.

## Current direction
All-synthetic training via a composite of generation sources; a controlled inverse (digital-twin)
alongside the uncontrolled (diversity) generator. See `core/data/dynamic/GENERATION.md` for the
architecture and `bd ready` for live work.
